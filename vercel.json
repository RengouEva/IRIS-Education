{
  "buildCommand": "cd backend && npm run build && cd ../app && npm run build",
  "outputDirectory": "app/dist",
  "installCommand": "npm install && cd app && npm install && cd ../backend && npm install",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "memory": 512
    }
  }
}
